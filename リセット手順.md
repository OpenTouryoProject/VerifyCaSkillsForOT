検証結果の SETUP-CHANGES.md を参考に。

- 必須
  - 環境変数をクリア  
    `[System.Environment]::SetEnvironmentVariable('OT_RESOURCE_ROOT', $null, 'User')`

- 任意
  - `C:\otr\` の削除（バージョン変更の場合は必須）
  - ASP.NET State Serviceの停止
  - LocalServicesOnDockerの停止